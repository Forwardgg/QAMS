=== COURSE MODEL === (finalized)
    create() - Creates new user with email validation and role checking
    findByEmail() - Finds user by email (includes password hash, for auth only)
    findPublicById() - Gets safe user data without password hash
    findById() - Gets full user data including password hash (internal use)
    getAll() - Gets all users with safe public data
    updateStatus() - Updates user status (active/inactive)
    updateProfile() - Updates user profile with email and role validation
    updatePassword() - Updates user password hash
    softDelete() - Soft deletes user by setting status to inactive
    forceDelete() - Hard deletes user from database

=== COURSE MODEL === (finalized)
    createCourse() - Creates a new course with validation, prevents duplicate codes
    getAllCourses() - Retrieves paginated courses with search, sorting and filtering
    getCourseById() - Finds a single course by its numeric ID
    getCourseByCode() - Finds a single course by its code string
    updateCourse() - Updates course fields with validation and conflict checking
    deleteCourse() - Removes a course and returns deleted record
    searchCourses() - Simplified search interface for finding courses

=== COURSE OUTCOME MODEL === (finalized)
    createCourseOutcome() - Creates a new course outcome with validation, prevents duplicates
    getAllCourseOutcomes() - Gets paginated outcomes with course details
    getCourseOutcomesByCourseCode() - Finds outcomes by course code
    getCourseOutcomesByCourseId() - Finds outcomes by course ID
    getCourseOutcomeByNumber() - Gets specific outcome by course ID and CO number
    getCourseOutcomeById() - Finds single outcome by its ID
    updateCourseOutcome() - Updates outcome fields with validation
    deleteCourseOutcome() - Removes outcome and returns deleted record
    searchCourseOutcomes() - Searches outcomes by course code and CO number

=== QUESTION PAPER MODEL ===
    create() - Creates a new question paper with validation, supports transactions
    getAll() - Gets all papers with course and creator details
    findById() - Gets paper by ID with full details
    getByCourseCode() - Gets papers by course code
    getByCourseAndCO() - Gets papers by course code and CO number
    update() - Updates paper fields with status validation, supports transactions
    delete() - Deletes paper and handles cascade deletion
    isOwner() - Checks if user owns the paper for authorization
    getPapersForModeration() - Gets papers available for moderation with filters
    getPaperForModeration() - Gets paper details with course outcomes for moderation
    updateStatus() - Updates paper status, supports transactions
    startModeration() - Starts moderation process for submitted papers
    getAllPapersForModerator() - Gets papers for moderators with exclusion logic

=== QUESTION MODEL ===
    create() - Creates a new question with validation, supports transactions
    update() - Updates question fields with status validation, supports transactions
    findById() - Gets question by ID with course and CO details
    findByPaperId() - Gets all questions for a paper in sequence order
    delete() - Deletes a question and returns deleted ID
    updateSequenceNumbers() - Updates question ordering for a paper
    extractMediaUrls() - Extracts image URLs from HTML content
    linkMedia() - Links media files to a question
    unlinkUnusedMedia() - Unlinks media no longer used by question
    getQuestionMedia() - Gets all media records for a question
    search() - Searches questions with filters and pagination
    getNextSequenceNumber() - Gets next sequence number for a paper
    validatePaperAccess() - Validates user access to paper
    validateCOForPaper() - Validates CO belongs to paper's course
    updateQuestionStatus() - Updates individual question status
    getQuestionsForModeration() - Gets questions with details for moderation
    bulkUpdateStatus() - Bulk updates question statuses
    getQuestionsByCO() - Groups questions by CO for moderation reports

=== QUESTION MEDIA MODEL ===
    validateFile() - Validates file type, size, and structure
    isBufferSignatureValid() - Validates file signature against MIME type
    generateFilename() - Generates safe filename with timestamp and random hash
    sanitizeFilename() - Sanitizes filename to prevent path traversal
    saveFileToDisk() - Saves file buffer to uploads directory
    deleteFileIfExists() - Deletes file from disk if it exists
    create() - Creates media record in database
    findById() - Finds media record by ID
    findByQuestionId() - Finds all media for a question
    findByPaperId() - Finds all media for a paper
    update() - Updates media record fields
    linkToQuestion() - Links media to a question and marks as used
    unlinkFromQuestion() - Unlinks media from question
    delete() - Hard deletes media record from database
    getPublicUrl() - Generates public URL for a filename
    extractMediaUrls() - Extracts image URLs from HTML content
    findByUrls() - Finds media records by URL list

=== MODERATION MODEL ===
    create() - Creates new moderation record, prevents duplicates, supports transactions
    findByPaper() - Gets latest moderation for a paper with details
    findPendingByPaper() - Finds active pending moderation for a paper
    update() - Updates moderation fields with validation, supports transactions
    getByModerator() - Gets all moderations by a specific moderator
    findById() - Gets moderation by ID with full details
    isModeratingPaper() - Checks if moderator is already moderating a paper
    isPaperBeingModerated() - Checks if paper has any pending moderation
    getPendingModerations() - Gets all pending moderations across system
    getModeratorStats() - Gets moderation statistics for a moderator
    getActiveModerations() - Gets active moderations for a moderator
    delete() - Deletes moderation record
    getAllModerations() - Gets all moderations with search, filters and pagination